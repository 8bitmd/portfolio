export default function WorkInProgress() {
    return (
        <div className={"work-in-progress"}>
            <p>⚠️ Ce portfolio ne sera plus maintenu. Une version 2 complètement repensée est en cours de création.</p>
        </div>
    )
}